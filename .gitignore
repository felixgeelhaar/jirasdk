# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Dependency directories
vendor/

# Go workspace file
go.work
go.work.sum

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env.*.local

# Build artifacts
bin/
dist/

# Example binaries (built from examples/*/)
/basic
/advanced
/workflow
/customfields
/attachments
/oauth2
.claude/

# release memory
.relicta/

# roady project state
.roady/

# Pinned dev tooling installed by scripts/lint.sh
.tools/

# nox scan output. These are regenerated by every scan and their content is
# finding fingerprints, so they also re-trip the secret detector when scanned —
# .nox.yaml excludes the same paths for that reason.
# .nox/baseline.json is NOT ignored: the waiver list is deliberately tracked.
findings.json
nox-results/
*.sarif
